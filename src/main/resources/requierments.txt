Create document collection payments with structure:
id
order_id
user_id
status
timestamp
payment_amount

Use Liquibase for automatic DB schema creation!
Add indexes!

Create entities for collection in DB.
Implement CRUD operations:
Create Payment
Get Payments by user_id or order_id or status (it could be user_id or order_id or status)
Get total sum of payments for date range for current user
Get total sum of payments for date range for all users (for admin)

Controller layer operates DTOs and DAO Layer operates entities.
Create DTOs for entities from DAO Layer. Use Mapstruct to convert DTO → Entity and Entity → DTO.
Add communication with external API to generate random number. If the number is even payment status will be SUCCESS, otherwise FAILED.
Add validation annotations for DTO.

Implement Unit tests for all Service Layer methods
Implement Integration tests using WireMock and TestContainers (MongoDB and Kafka)